import styled from 'styled-components';

export const Button = styled.a`
    font-weight: 400;
    font-size: 16px;
    color: var(--Pale-Blue);

    &:hover{
        color: var(--Text-active-color);
    }
`;